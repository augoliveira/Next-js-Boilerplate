import { Inter as FontSans, Open_Sans as Montserrat } from 'next/font/google';

export const fontSans = FontSans({
  subsets: ['latin'],
  variable: '--font-sans',
});

export const montserrat = Montserrat({
  subsets: ['latin'],
  variable: '--font-sans',
});
